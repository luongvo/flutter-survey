name: Create the Release pull request

on:
  workflow_dispatch:
  # testing
  push:
    branches:
      - feature/create-release-pr-workflow

jobs:
  create_release_pr:
    name: Create the Release pull request
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout the latest code
        uses: actions/checkout@v3

      - name: Read the current version
        id: version
        uses: christian-draeger/read-properties@1.1.1
        with:
          path: "version.properties"
          properties: "templateScriptVersion"

      - uses: nimblehq/github-actions-workflows/create_release_pr@0.1.8
        with:
          version: ${{ steps.version.outputs.templateScriptVersion }}
          # base_branch: main-0.6
          changelogConfiguration: ".github/workflows/config/changelog-release.json"
          # assignee: luongvo-bot
